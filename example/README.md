# Toast Example

这是当前 `toast` 包的可运行示例项目。

## 运行

```bash
cd example
flutter run
```

指定平台运行：

```bash
flutter run -d macos
flutter run -d chrome
flutter run -d ios
flutter run -d android
```

## 测试

```bash
cd example
flutter test
```
